name: Podcast Generator
author: Nicolas Fränkel
description: Generates a RSS feed for a podcast from a YAML file
runs:
  using: docker
  image: Dockerfile
branding:
  icon: git-branch
  color: red
inputs:
  email: 
    description: Committer's email address
    required: true
    default: ${{ github.actor }}@localhost
  name:
    description: Committer's name
    required: true
    default: ${{ github.actor }}
